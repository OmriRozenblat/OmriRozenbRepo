#include <stdio.h>
#include <stdbool.h>
#define ABC_LEN 26



int q4 (char *s);
int q4_aux (char *s);
int str_len(char * str);

int main() {
    char arr[] = {'a','b','a','b','c'};
    int res = q3(arr, 5);
    printf("%d", res);
    return 0;
}

int q4_aux (char *s, )
